<header class="bg-red-500 text-white p-4 shadow-md">
  <nav class="container mx-auto flex justify-between items-center">

    <!-- Logo -->
    <a href="/" class="text-xl font-bold">Airbnb</a>

    <!-- Mobile Hamburger Button -->
    <button id="menu-toggle" class="md:hidden block focus:outline-none">
      <svg
        class="w-6 h-6"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M4 6h16M4 12h16m-7 6h7"
        ></path>
      </svg>
    </button>

    <!-- Navigation Links -->
    <ul
      id="menu"
      class="hidden md:flex flex-col md:flex-row md:space-x-4 absolute md:static top-16 left-0 w-full bg-red-500 md:w-auto p-4 md:p-0 z-10"
    >

      <!-- Always visible -->
      <li>
        <a
          href="/"
          class="<%= currentPage === 'index' ? 'bg-red-400' : 'hover:bg-red-400' %> block py-2 px-4 rounded transition duration-300"
        >
          Airbnb
        </a>
      </li>

      <% if (isLoggedIn) { %>

        <!-- Logged-in user links -->

        <li>
          <a
            href="/homes"
            class="<%= currentPage === 'Home' ? 'bg-red-400' : 'hover:bg-red-400' %> block py-2 px-4 rounded transition duration-300"
          >
            Homes-List
          </a>
        </li>

        <li>
          <a
            href="/host/add-home"
            class="<%= currentPage === 'addHome' ? 'bg-red-400' : 'hover:bg-red-400' %> block py-2 px-4 rounded transition duration-300"
          >
            Add Home
          </a>
        </li>

        <li>
          <a
            href="/favourites"
            class="<%= currentPage === 'favourites' ? 'bg-red-400' : 'hover:bg-red-400' %> block py-2 px-4 rounded transition duration-300"
          >
            Favourites
          </a>
        </li>

        <li>
          <a
            href="/bookings"
            class="<%= currentPage === 'bookings' ? 'bg-red-400' : 'hover:bg-red-400' %> block py-2 px-4 rounded transition duration-300"
          >
            Bookings
          </a>
        </li>

        <li>
          <a
            href="/host/host-home-list"
            class="<%= currentPage === 'host-homes' ? 'bg-red-400' : 'hover:bg-red-400' %> block py-2 px-4 rounded transition duration-300"
          >
            Host Homes
          </a>
        </li>

        <!-- Logout -->
        <li>
          <form action="/logout" method="POST">
            <button
              type="submit"
              class="hover:bg-red-400 block py-2 px-4 rounded transition duration-300"
            >
              Logout
            </button>
          </form>
        </li>

      <% } else { %>

        <!-- Logged-out user -->

        <li>
          <a
            href="/login"
            class="<%= currentPage === 'login' ? 'bg-red-400' : 'hover:bg-red-400' %> block py-2 px-4 rounded transition duration-300"
          >
            Login
          </a>
        </li>

      <% } %>

    </ul>
  </nav>
</header>

<script>
  // Toggle mobile menu
  document
    .getElementById('menu-toggle')
    .addEventListener('click', function () {
      const menu = document.getElementById('menu');
      menu.classList.toggle('hidden');
    });
</script>